# Copy this to .env.local and fill in your Supabase project's values
# (Project Settings -> API in the Supabase dashboard). Both are safe to
# expose client-side — row-level security in supabase/schema.sql is what
# actually protects data.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
